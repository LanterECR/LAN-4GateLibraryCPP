name: Linux

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-18.04]
        compiler:
          - name: "gcc-5"
            package: "gcc-5"
          - name: "gcc-6"
            package: "gcc-6"
          - name: "gcc-7"
            package: "gcc-7"
          - name: "gcc-8"
            package: "gcc-8"
          - name: "gcc-9"
            package: "gcc-9"
          - name: "clang-5"
            package: "clang-5"
          - name: "clang-6"
            package: "clang-6"
          - name: "clang-7"
            package: "clang-7"
          - name: "clang-8"
            package: "clang-8"
          - name: "clang-9"
            package: "clang-9"
          - name: "clang-10"
            package: "clang-10"
        build_type: ["Debug", "Release"]

    runs-on: ${{matrix.os}}
    steps:
    - uses: actions/checkout@v2

    - name: Add APT repositories
      run: |
        wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc 2>/dev/null | sudo apt-key add -
        sudo apt-add-repository 'deb https://apt.kitware.com/ubuntu/ bionic main'

    - name: Install APT dependencies
      run: |
        sudo apt update
        sudo apt install --yes ${{matrix.compiler.package}} git cmake

    - name: Configure
      run: |
        cmake -S . -B Build/mylib -DCMAKE_BUILD_TYPE=${{matrix.build_type}} -DCMAKE_CXX_COMPILER=${{matrix.compiler.name}} -DL4G_TESTS=ON

    - name: Build and test
      run: |
        cmake --build Build/mylib
        ctest
